# FigureSmith — build desktop app (PLACEHOLDER)
#
# Phase: Phase 4 (Tauri desktop shell) — not implemented in Phase 1
#
# Intended future responsibilities:
# - Build Tauri app under apps/desktop/
# - Wire backend lifecycle to 127.0.0.1 only
# - Produce installer artifacts (without shipping model weights by default)
#
# Phase 1: no-op with guidance only.

$ErrorActionPreference = "Stop"

Write-Host "build-desktop.ps1 is a Phase 1 placeholder." -ForegroundColor Yellow
Write-Host "Desktop/Tauri packaging is scheduled for Phase 4."
Write-Host "For now use: ./scripts/run-backend.ps1 and the vendor web UI."
Write-Host "See apps/desktop/README.md"
exit 1
